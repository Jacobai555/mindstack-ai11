// node_modules/@tanstack/ai/dist/esm/activities/chat/adapter.js
var BaseTextAdapter = class {
  constructor(config = {}, model) {
    this.kind = "text";
    this.config = config;
    this.model = model;
  }
  generateId() {
    return `${this.name}-${Date.now()}-${Math.random().toString(36).substring(7)}`;
  }
};

// node_modules/@tanstack/ai/dist/esm/activities/summarize/adapter.js
var BaseSummarizeAdapter = class {
  constructor(config = {}, model) {
    this.kind = "summarize";
    this.config = config;
    this.model = model;
  }
  generateId() {
    return `${this.name}-${Date.now()}-${Math.random().toString(36).substring(7)}`;
  }
};

// node_modules/@tanstack/ai/dist/esm/activities/generateImage/adapter.js
var BaseImageAdapter = class {
  constructor(config = {}, model) {
    this.kind = "image";
    this.config = config;
    this.model = model;
  }
  generateId() {
    return `${this.name}-${Date.now()}-${Math.random().toString(36).substring(7)}`;
  }
};

// node_modules/@tanstack/ai/dist/esm/activities/generateVideo/adapter.js
var BaseVideoAdapter = class {
  constructor(config = {}, model) {
    this.kind = "video";
    this.config = config;
    this.model = model;
  }
  generateId() {
    return `${this.name}-${Date.now()}-${Math.random().toString(36).substring(7)}`;
  }
};

// node_modules/@tanstack/ai/dist/esm/activities/generateSpeech/adapter.js
var BaseTTSAdapter = class {
  constructor(config = {}, model) {
    this.kind = "tts";
    this.config = config;
    this.model = model;
  }
  generateId() {
    return `${this.name}-${Date.now()}-${Math.random().toString(36).substring(7)}`;
  }
};

// node_modules/@tanstack/ai/dist/esm/activities/generateTranscription/adapter.js
var BaseTranscriptionAdapter = class {
  constructor(config = {}, model) {
    this.kind = "transcription";
    this.config = config;
    this.model = model;
  }
  generateId() {
    return `${this.name}-${Date.now()}-${Math.random().toString(36).substring(7)}`;
  }
};

export {
  BaseTextAdapter,
  BaseSummarizeAdapter,
  BaseImageAdapter,
  BaseVideoAdapter,
  BaseTTSAdapter,
  BaseTranscriptionAdapter
};
//# sourceMappingURL=chunk-XY7V2F4D.js.map
