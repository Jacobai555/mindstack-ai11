import {
  BaseTextAdapter
} from "./chunk-XY7V2F4D.js";
import "./chunk-QB6SBWHX.js";
import {
  __commonJS,
  __toESM
} from "./chunk-G3PMV62Z.js";

// browser-external:node:fs
var require_node_fs = __commonJS({
  "browser-external:node:fs"(exports, module) {
    module.exports = Object.create(new Proxy({}, {
      get(_, key) {
        if (key !== "__esModule" && key !== "__proto__" && key !== "constructor" && key !== "splice") {
          console.warn(`Module "node:fs" has been externalized for browser compatibility. Cannot access "node:fs.${key}" in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details.`);
        }
      }
    }));
  }
});

// browser-external:node:path
var require_node_path = __commonJS({
  "browser-external:node:path"(exports, module) {
    module.exports = Object.create(new Proxy({}, {
      get(_, key) {
        if (key !== "__esModule" && key !== "__proto__" && key !== "constructor" && key !== "splice") {
          console.warn(`Module "node:path" has been externalized for browser compatibility. Cannot access "node:path.${key}" in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details.`);
        }
      }
    }));
  }
});

// node_modules/ollama/dist/index.mjs
var import_node_fs = __toESM(require_node_fs(), 1);
var import_node_path = __toESM(require_node_path(), 1);

// node_modules/whatwg-fetch/fetch.js
var g = typeof globalThis !== "undefined" && globalThis || typeof self !== "undefined" && self || // eslint-disable-next-line no-undef
typeof global !== "undefined" && global || {};
var support = {
  searchParams: "URLSearchParams" in g,
  iterable: "Symbol" in g && "iterator" in Symbol,
  blob: "FileReader" in g && "Blob" in g && (function() {
    try {
      new Blob();
      return true;
    } catch (e) {
      return false;
    }
  })(),
  formData: "FormData" in g,
  arrayBuffer: "ArrayBuffer" in g
};
function isDataView(obj) {
  return obj && DataView.prototype.isPrototypeOf(obj);
}
if (support.arrayBuffer) {
  viewClasses = [
    "[object Int8Array]",
    "[object Uint8Array]",
    "[object Uint8ClampedArray]",
    "[object Int16Array]",
    "[object Uint16Array]",
    "[object Int32Array]",
    "[object Uint32Array]",
    "[object Float32Array]",
    "[object Float64Array]"
  ];
  isArrayBufferView = ArrayBuffer.isView || function(obj) {
    return obj && viewClasses.indexOf(Object.prototype.toString.call(obj)) > -1;
  };
}
var viewClasses;
var isArrayBufferView;
function normalizeName(name) {
  if (typeof name !== "string") {
    name = String(name);
  }
  if (/[^a-z0-9\-#$%&'*+.^_`|~!]/i.test(name) || name === "") {
    throw new TypeError('Invalid character in header field name: "' + name + '"');
  }
  return name.toLowerCase();
}
function normalizeValue(value) {
  if (typeof value !== "string") {
    value = String(value);
  }
  return value;
}
function iteratorFor(items) {
  var iterator = {
    next: function() {
      var value = items.shift();
      return { done: value === void 0, value };
    }
  };
  if (support.iterable) {
    iterator[Symbol.iterator] = function() {
      return iterator;
    };
  }
  return iterator;
}
function Headers2(headers) {
  this.map = {};
  if (headers instanceof Headers2) {
    headers.forEach(function(value, name) {
      this.append(name, value);
    }, this);
  } else if (Array.isArray(headers)) {
    headers.forEach(function(header) {
      if (header.length != 2) {
        throw new TypeError("Headers constructor: expected name/value pair to be length 2, found" + header.length);
      }
      this.append(header[0], header[1]);
    }, this);
  } else if (headers) {
    Object.getOwnPropertyNames(headers).forEach(function(name) {
      this.append(name, headers[name]);
    }, this);
  }
}
Headers2.prototype.append = function(name, value) {
  name = normalizeName(name);
  value = normalizeValue(value);
  var oldValue = this.map[name];
  this.map[name] = oldValue ? oldValue + ", " + value : value;
};
Headers2.prototype["delete"] = function(name) {
  delete this.map[normalizeName(name)];
};
Headers2.prototype.get = function(name) {
  name = normalizeName(name);
  return this.has(name) ? this.map[name] : null;
};
Headers2.prototype.has = function(name) {
  return this.map.hasOwnProperty(normalizeName(name));
};
Headers2.prototype.set = function(name, value) {
  this.map[normalizeName(name)] = normalizeValue(value);
};
Headers2.prototype.forEach = function(callback, thisArg) {
  for (var name in this.map) {
    if (this.map.hasOwnProperty(name)) {
      callback.call(thisArg, this.map[name], name, this);
    }
  }
};
Headers2.prototype.keys = function() {
  var items = [];
  this.forEach(function(value, name) {
    items.push(name);
  });
  return iteratorFor(items);
};
Headers2.prototype.values = function() {
  var items = [];
  this.forEach(function(value) {
    items.push(value);
  });
  return iteratorFor(items);
};
Headers2.prototype.entries = function() {
  var items = [];
  this.forEach(function(value, name) {
    items.push([name, value]);
  });
  return iteratorFor(items);
};
if (support.iterable) {
  Headers2.prototype[Symbol.iterator] = Headers2.prototype.entries;
}
function consumed(body) {
  if (body._noBody) return;
  if (body.bodyUsed) {
    return Promise.reject(new TypeError("Already read"));
  }
  body.bodyUsed = true;
}
function fileReaderReady(reader) {
  return new Promise(function(resolve2, reject) {
    reader.onload = function() {
      resolve2(reader.result);
    };
    reader.onerror = function() {
      reject(reader.error);
    };
  });
}
function readBlobAsArrayBuffer(blob) {
  var reader = new FileReader();
  var promise = fileReaderReady(reader);
  reader.readAsArrayBuffer(blob);
  return promise;
}
function readBlobAsText(blob) {
  var reader = new FileReader();
  var promise = fileReaderReady(reader);
  var match = /charset=([A-Za-z0-9_-]+)/.exec(blob.type);
  var encoding = match ? match[1] : "utf-8";
  reader.readAsText(blob, encoding);
  return promise;
}
function readArrayBufferAsText(buf) {
  var view = new Uint8Array(buf);
  var chars = new Array(view.length);
  for (var i = 0; i < view.length; i++) {
    chars[i] = String.fromCharCode(view[i]);
  }
  return chars.join("");
}
function bufferClone(buf) {
  if (buf.slice) {
    return buf.slice(0);
  } else {
    var view = new Uint8Array(buf.byteLength);
    view.set(new Uint8Array(buf));
    return view.buffer;
  }
}
function Body() {
  this.bodyUsed = false;
  this._initBody = function(body) {
    this.bodyUsed = this.bodyUsed;
    this._bodyInit = body;
    if (!body) {
      this._noBody = true;
      this._bodyText = "";
    } else if (typeof body === "string") {
      this._bodyText = body;
    } else if (support.blob && Blob.prototype.isPrototypeOf(body)) {
      this._bodyBlob = body;
    } else if (support.formData && FormData.prototype.isPrototypeOf(body)) {
      this._bodyFormData = body;
    } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
      this._bodyText = body.toString();
    } else if (support.arrayBuffer && support.blob && isDataView(body)) {
      this._bodyArrayBuffer = bufferClone(body.buffer);
      this._bodyInit = new Blob([this._bodyArrayBuffer]);
    } else if (support.arrayBuffer && (ArrayBuffer.prototype.isPrototypeOf(body) || isArrayBufferView(body))) {
      this._bodyArrayBuffer = bufferClone(body);
    } else {
      this._bodyText = body = Object.prototype.toString.call(body);
    }
    if (!this.headers.get("content-type")) {
      if (typeof body === "string") {
        this.headers.set("content-type", "text/plain;charset=UTF-8");
      } else if (this._bodyBlob && this._bodyBlob.type) {
        this.headers.set("content-type", this._bodyBlob.type);
      } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
        this.headers.set("content-type", "application/x-www-form-urlencoded;charset=UTF-8");
      }
    }
  };
  if (support.blob) {
    this.blob = function() {
      var rejected = consumed(this);
      if (rejected) {
        return rejected;
      }
      if (this._bodyBlob) {
        return Promise.resolve(this._bodyBlob);
      } else if (this._bodyArrayBuffer) {
        return Promise.resolve(new Blob([this._bodyArrayBuffer]));
      } else if (this._bodyFormData) {
        throw new Error("could not read FormData body as blob");
      } else {
        return Promise.resolve(new Blob([this._bodyText]));
      }
    };
  }
  this.arrayBuffer = function() {
    if (this._bodyArrayBuffer) {
      var isConsumed = consumed(this);
      if (isConsumed) {
        return isConsumed;
      } else if (ArrayBuffer.isView(this._bodyArrayBuffer)) {
        return Promise.resolve(
          this._bodyArrayBuffer.buffer.slice(
            this._bodyArrayBuffer.byteOffset,
            this._bodyArrayBuffer.byteOffset + this._bodyArrayBuffer.byteLength
          )
        );
      } else {
        return Promise.resolve(this._bodyArrayBuffer);
      }
    } else if (support.blob) {
      return this.blob().then(readBlobAsArrayBuffer);
    } else {
      throw new Error("could not read as ArrayBuffer");
    }
  };
  this.text = function() {
    var rejected = consumed(this);
    if (rejected) {
      return rejected;
    }
    if (this._bodyBlob) {
      return readBlobAsText(this._bodyBlob);
    } else if (this._bodyArrayBuffer) {
      return Promise.resolve(readArrayBufferAsText(this._bodyArrayBuffer));
    } else if (this._bodyFormData) {
      throw new Error("could not read FormData body as text");
    } else {
      return Promise.resolve(this._bodyText);
    }
  };
  if (support.formData) {
    this.formData = function() {
      return this.text().then(decode);
    };
  }
  this.json = function() {
    return this.text().then(JSON.parse);
  };
  return this;
}
var methods = ["CONNECT", "DELETE", "GET", "HEAD", "OPTIONS", "PATCH", "POST", "PUT", "TRACE"];
function normalizeMethod(method) {
  var upcased = method.toUpperCase();
  return methods.indexOf(upcased) > -1 ? upcased : method;
}
function Request(input, options) {
  if (!(this instanceof Request)) {
    throw new TypeError('Please use the "new" operator, this DOM object constructor cannot be called as a function.');
  }
  options = options || {};
  var body = options.body;
  if (input instanceof Request) {
    if (input.bodyUsed) {
      throw new TypeError("Already read");
    }
    this.url = input.url;
    this.credentials = input.credentials;
    if (!options.headers) {
      this.headers = new Headers2(input.headers);
    }
    this.method = input.method;
    this.mode = input.mode;
    this.signal = input.signal;
    if (!body && input._bodyInit != null) {
      body = input._bodyInit;
      input.bodyUsed = true;
    }
  } else {
    this.url = String(input);
  }
  this.credentials = options.credentials || this.credentials || "same-origin";
  if (options.headers || !this.headers) {
    this.headers = new Headers2(options.headers);
  }
  this.method = normalizeMethod(options.method || this.method || "GET");
  this.mode = options.mode || this.mode || null;
  this.signal = options.signal || this.signal || (function() {
    if ("AbortController" in g) {
      var ctrl = new AbortController();
      return ctrl.signal;
    }
  })();
  this.referrer = null;
  if ((this.method === "GET" || this.method === "HEAD") && body) {
    throw new TypeError("Body not allowed for GET or HEAD requests");
  }
  this._initBody(body);
  if (this.method === "GET" || this.method === "HEAD") {
    if (options.cache === "no-store" || options.cache === "no-cache") {
      var reParamSearch = /([?&])_=[^&]*/;
      if (reParamSearch.test(this.url)) {
        this.url = this.url.replace(reParamSearch, "$1_=" + (/* @__PURE__ */ new Date()).getTime());
      } else {
        var reQueryString = /\?/;
        this.url += (reQueryString.test(this.url) ? "&" : "?") + "_=" + (/* @__PURE__ */ new Date()).getTime();
      }
    }
  }
}
Request.prototype.clone = function() {
  return new Request(this, { body: this._bodyInit });
};
function decode(body) {
  var form = new FormData();
  body.trim().split("&").forEach(function(bytes) {
    if (bytes) {
      var split = bytes.split("=");
      var name = split.shift().replace(/\+/g, " ");
      var value = split.join("=").replace(/\+/g, " ");
      form.append(decodeURIComponent(name), decodeURIComponent(value));
    }
  });
  return form;
}
function parseHeaders(rawHeaders) {
  var headers = new Headers2();
  var preProcessedHeaders = rawHeaders.replace(/\r?\n[\t ]+/g, " ");
  preProcessedHeaders.split("\r").map(function(header) {
    return header.indexOf("\n") === 0 ? header.substr(1, header.length) : header;
  }).forEach(function(line) {
    var parts = line.split(":");
    var key = parts.shift().trim();
    if (key) {
      var value = parts.join(":").trim();
      try {
        headers.append(key, value);
      } catch (error) {
        console.warn("Response " + error.message);
      }
    }
  });
  return headers;
}
Body.call(Request.prototype);
function Response(bodyInit, options) {
  if (!(this instanceof Response)) {
    throw new TypeError('Please use the "new" operator, this DOM object constructor cannot be called as a function.');
  }
  if (!options) {
    options = {};
  }
  this.type = "default";
  this.status = options.status === void 0 ? 200 : options.status;
  if (this.status < 200 || this.status > 599) {
    throw new RangeError("Failed to construct 'Response': The status provided (0) is outside the range [200, 599].");
  }
  this.ok = this.status >= 200 && this.status < 300;
  this.statusText = options.statusText === void 0 ? "" : "" + options.statusText;
  this.headers = new Headers2(options.headers);
  this.url = options.url || "";
  this._initBody(bodyInit);
}
Body.call(Response.prototype);
Response.prototype.clone = function() {
  return new Response(this._bodyInit, {
    status: this.status,
    statusText: this.statusText,
    headers: new Headers2(this.headers),
    url: this.url
  });
};
Response.error = function() {
  var response = new Response(null, { status: 200, statusText: "" });
  response.ok = false;
  response.status = 0;
  response.type = "error";
  return response;
};
var redirectStatuses = [301, 302, 303, 307, 308];
Response.redirect = function(url, status) {
  if (redirectStatuses.indexOf(status) === -1) {
    throw new RangeError("Invalid status code");
  }
  return new Response(null, { status, headers: { location: url } });
};
var DOMException = g.DOMException;
try {
  new DOMException();
} catch (err) {
  DOMException = function(message, name) {
    this.message = message;
    this.name = name;
    var error = Error(message);
    this.stack = error.stack;
  };
  DOMException.prototype = Object.create(Error.prototype);
  DOMException.prototype.constructor = DOMException;
}
function fetch2(input, init) {
  return new Promise(function(resolve2, reject) {
    var request = new Request(input, init);
    if (request.signal && request.signal.aborted) {
      return reject(new DOMException("Aborted", "AbortError"));
    }
    var xhr = new XMLHttpRequest();
    function abortXhr() {
      xhr.abort();
    }
    xhr.onload = function() {
      var options = {
        statusText: xhr.statusText,
        headers: parseHeaders(xhr.getAllResponseHeaders() || "")
      };
      if (request.url.indexOf("file://") === 0 && (xhr.status < 200 || xhr.status > 599)) {
        options.status = 200;
      } else {
        options.status = xhr.status;
      }
      options.url = "responseURL" in xhr ? xhr.responseURL : options.headers.get("X-Request-URL");
      var body = "response" in xhr ? xhr.response : xhr.responseText;
      setTimeout(function() {
        resolve2(new Response(body, options));
      }, 0);
    };
    xhr.onerror = function() {
      setTimeout(function() {
        reject(new TypeError("Network request failed"));
      }, 0);
    };
    xhr.ontimeout = function() {
      setTimeout(function() {
        reject(new TypeError("Network request timed out"));
      }, 0);
    };
    xhr.onabort = function() {
      setTimeout(function() {
        reject(new DOMException("Aborted", "AbortError"));
      }, 0);
    };
    function fixUrl(url) {
      try {
        return url === "" && g.location.href ? g.location.href : url;
      } catch (e) {
        return url;
      }
    }
    xhr.open(request.method, fixUrl(request.url), true);
    if (request.credentials === "include") {
      xhr.withCredentials = true;
    } else if (request.credentials === "omit") {
      xhr.withCredentials = false;
    }
    if ("responseType" in xhr) {
      if (support.blob) {
        xhr.responseType = "blob";
      } else if (support.arrayBuffer) {
        xhr.responseType = "arraybuffer";
      }
    }
    if (init && typeof init.headers === "object" && !(init.headers instanceof Headers2 || g.Headers && init.headers instanceof g.Headers)) {
      var names = [];
      Object.getOwnPropertyNames(init.headers).forEach(function(name) {
        names.push(normalizeName(name));
        xhr.setRequestHeader(name, normalizeValue(init.headers[name]));
      });
      request.headers.forEach(function(value, name) {
        if (names.indexOf(name) === -1) {
          xhr.setRequestHeader(name, value);
        }
      });
    } else {
      request.headers.forEach(function(value, name) {
        xhr.setRequestHeader(name, value);
      });
    }
    if (request.signal) {
      request.signal.addEventListener("abort", abortXhr);
      xhr.onreadystatechange = function() {
        if (xhr.readyState === 4) {
          request.signal.removeEventListener("abort", abortXhr);
        }
      };
    }
    xhr.send(typeof request._bodyInit === "undefined" ? null : request._bodyInit);
  });
}
fetch2.polyfill = true;
if (!g.fetch) {
  g.fetch = fetch2;
  g.Headers = Headers2;
  g.Request = Request;
  g.Response = Response;
}

// node_modules/ollama/dist/browser.mjs
var defaultPort = "11434";
var defaultHost = `http://127.0.0.1:${defaultPort}`;
var version = "0.6.3";
var __defProp$1 = Object.defineProperty;
var __defNormalProp$1 = (obj, key, value) => key in obj ? __defProp$1(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField$1 = (obj, key, value) => {
  __defNormalProp$1(obj, typeof key !== "symbol" ? key + "" : key, value);
  return value;
};
var ResponseError = class _ResponseError extends Error {
  constructor(error, status_code) {
    super(error);
    this.error = error;
    this.status_code = status_code;
    this.name = "ResponseError";
    if (Error.captureStackTrace) {
      Error.captureStackTrace(this, _ResponseError);
    }
  }
};
var AbortableAsyncIterator = class {
  constructor(abortController, itr, doneCallback) {
    __publicField$1(this, "abortController");
    __publicField$1(this, "itr");
    __publicField$1(this, "doneCallback");
    this.abortController = abortController;
    this.itr = itr;
    this.doneCallback = doneCallback;
  }
  abort() {
    this.abortController.abort();
  }
  async *[Symbol.asyncIterator]() {
    for await (const message of this.itr) {
      if ("error" in message) {
        throw new Error(message.error);
      }
      yield message;
      if (message.done || message.status === "success") {
        this.doneCallback();
        return;
      }
    }
    throw new Error("Did not receive done or success response in stream.");
  }
};
var checkOk = async (response) => {
  if (response.ok) {
    return;
  }
  let message = `Error ${response.status}: ${response.statusText}`;
  let errorData = null;
  if (response.headers.get("content-type")?.includes("application/json")) {
    try {
      errorData = await response.json();
      message = errorData.error || message;
    } catch (error) {
      console.log("Failed to parse error response as JSON");
    }
  } else {
    try {
      console.log("Getting text from response");
      const textResponse = await response.text();
      message = textResponse || message;
    } catch (error) {
      console.log("Failed to get text from error response");
    }
  }
  throw new ResponseError(message, response.status);
};
function getPlatform() {
  if (typeof window !== "undefined" && window.navigator) {
    const nav = navigator;
    if ("userAgentData" in nav && nav.userAgentData?.platform) {
      return `${nav.userAgentData.platform.toLowerCase()} Browser/${navigator.userAgent};`;
    }
    if (navigator.platform) {
      return `${navigator.platform.toLowerCase()} Browser/${navigator.userAgent};`;
    }
    return `unknown Browser/${navigator.userAgent};`;
  } else if (typeof process !== "undefined") {
    return `${process.arch} ${process.platform} Node.js/${process.version}`;
  }
  return "";
}
function normalizeHeaders(headers) {
  if (headers instanceof Headers) {
    const obj = {};
    headers.forEach((value, key) => {
      obj[key] = value;
    });
    return obj;
  } else if (Array.isArray(headers)) {
    return Object.fromEntries(headers);
  } else {
    return headers || {};
  }
}
var readEnvVar = (obj, key) => {
  return obj[key];
};
var fetchWithHeaders = async (fetch3, url, options = {}) => {
  const defaultHeaders = {
    "Content-Type": "application/json",
    Accept: "application/json",
    "User-Agent": `ollama-js/${version} (${getPlatform()})`
  };
  options.headers = normalizeHeaders(options.headers);
  try {
    const parsed = new URL(url);
    if (parsed.protocol === "https:" && parsed.hostname === "ollama.com") {
      const apiKey = typeof process === "object" && process !== null && typeof process.env === "object" && process.env !== null ? readEnvVar(process.env, "OLLAMA_API_KEY") : void 0;
      const authorization = options.headers["authorization"] || options.headers["Authorization"];
      if (!authorization && apiKey) {
        options.headers["Authorization"] = `Bearer ${apiKey}`;
      }
    }
  } catch (error) {
    console.error("error parsing url", error);
  }
  const customHeaders = Object.fromEntries(
    Object.entries(options.headers).filter(
      ([key]) => !Object.keys(defaultHeaders).some(
        (defaultKey) => defaultKey.toLowerCase() === key.toLowerCase()
      )
    )
  );
  options.headers = {
    ...defaultHeaders,
    ...customHeaders
  };
  return fetch3(url, options);
};
var get = async (fetch3, host, options) => {
  const response = await fetchWithHeaders(fetch3, host, {
    headers: options?.headers
  });
  await checkOk(response);
  return response;
};
var post = async (fetch3, host, data, options) => {
  const isRecord = (input) => {
    return input !== null && typeof input === "object" && !Array.isArray(input);
  };
  const formattedData = isRecord(data) ? JSON.stringify(data) : data;
  const response = await fetchWithHeaders(fetch3, host, {
    method: "POST",
    body: formattedData,
    signal: options?.signal,
    headers: options?.headers
  });
  await checkOk(response);
  return response;
};
var del = async (fetch3, host, data, options) => {
  const response = await fetchWithHeaders(fetch3, host, {
    method: "DELETE",
    body: JSON.stringify(data),
    headers: options?.headers
  });
  await checkOk(response);
  return response;
};
var parseJSON = async function* (itr) {
  const decoder = new TextDecoder("utf-8");
  let buffer = "";
  const reader = itr.getReader();
  while (true) {
    const { done, value: chunk } = await reader.read();
    if (done) {
      break;
    }
    buffer += decoder.decode(chunk, { stream: true });
    const parts = buffer.split("\n");
    buffer = parts.pop() ?? "";
    for (const part of parts) {
      try {
        yield JSON.parse(part);
      } catch (error) {
        console.warn("invalid json: ", part);
      }
    }
  }
  buffer += decoder.decode();
  for (const part of buffer.split("\n").filter((p) => p !== "")) {
    try {
      yield JSON.parse(part);
    } catch (error) {
      console.warn("invalid json: ", part);
    }
  }
};
var formatHost = (host) => {
  if (!host) {
    return defaultHost;
  }
  let isExplicitProtocol = host.includes("://");
  if (host.startsWith(":")) {
    host = `http://127.0.0.1${host}`;
    isExplicitProtocol = true;
  }
  if (!isExplicitProtocol) {
    host = `http://${host}`;
  }
  const url = new URL(host);
  let port = url.port;
  if (!port) {
    if (!isExplicitProtocol) {
      port = defaultPort;
    } else {
      port = url.protocol === "https:" ? "443" : "80";
    }
  }
  let auth = "";
  if (url.username) {
    auth = url.username;
    if (url.password) {
      auth += `:${url.password}`;
    }
    auth += "@";
  }
  let formattedHost = `${url.protocol}//${auth}${url.hostname}:${port}${url.pathname}`;
  if (formattedHost.endsWith("/")) {
    formattedHost = formattedHost.slice(0, -1);
  }
  return formattedHost;
};
var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => {
  __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
  return value;
};
var Ollama$1 = class Ollama {
  constructor(config) {
    __publicField(this, "config");
    __publicField(this, "fetch");
    __publicField(this, "ongoingStreamedRequests", []);
    this.config = {
      host: "",
      headers: config?.headers
    };
    if (!config?.proxy) {
      this.config.host = formatHost(config?.host ?? defaultHost);
    }
    this.fetch = config?.fetch ?? fetch;
  }
  // Abort any ongoing streamed requests to Ollama
  abort() {
    for (const request of this.ongoingStreamedRequests) {
      request.abort();
    }
    this.ongoingStreamedRequests.length = 0;
  }
  /**
   * Processes a request to the Ollama server. If the request is streamable, it will return a
   * AbortableAsyncIterator that yields the response messages. Otherwise, it will return the response
   * object.
   * @param endpoint {string} - The endpoint to send the request to.
   * @param request {object} - The request object to send to the endpoint.
   * @protected {T | AbortableAsyncIterator<T>} - The response object or a AbortableAsyncIterator that yields
   * response messages.
   * @throws {Error} - If the response body is missing or if the response is an error.
   * @returns {Promise<T | AbortableAsyncIterator<T>>} - The response object or a AbortableAsyncIterator that yields the streamed response.
   */
  async processStreamableRequest(endpoint, request) {
    request.stream = request.stream ?? false;
    const host = `${this.config.host}/api/${endpoint}`;
    if (request.stream) {
      const abortController = new AbortController();
      const response2 = await post(this.fetch, host, request, {
        signal: abortController.signal,
        headers: this.config.headers
      });
      if (!response2.body) {
        throw new Error("Missing body");
      }
      const itr = parseJSON(response2.body);
      const abortableAsyncIterator = new AbortableAsyncIterator(
        abortController,
        itr,
        () => {
          const i = this.ongoingStreamedRequests.indexOf(abortableAsyncIterator);
          if (i > -1) {
            this.ongoingStreamedRequests.splice(i, 1);
          }
        }
      );
      this.ongoingStreamedRequests.push(abortableAsyncIterator);
      return abortableAsyncIterator;
    }
    const response = await post(this.fetch, host, request, {
      headers: this.config.headers
    });
    return await response.json();
  }
  /**
   * Encodes an image to base64 if it is a Uint8Array.
   * @param image {Uint8Array | string} - The image to encode.
   * @returns {Promise<string>} - The base64 encoded image.
   */
  async encodeImage(image) {
    if (typeof image !== "string") {
      const uint8Array = new Uint8Array(image);
      let byteString = "";
      const len = uint8Array.byteLength;
      for (let i = 0; i < len; i++) {
        byteString += String.fromCharCode(uint8Array[i]);
      }
      return btoa(byteString);
    }
    return image;
  }
  /**
   * Generates a response from a text prompt.
   * @param request {GenerateRequest} - The request object.
   * @returns {Promise<GenerateResponse | AbortableAsyncIterator<GenerateResponse>>} - The response object or
   * an AbortableAsyncIterator that yields response messages.
   */
  async generate(request) {
    if (request.images) {
      request.images = await Promise.all(request.images.map(this.encodeImage.bind(this)));
    }
    return this.processStreamableRequest("generate", request);
  }
  /**
   * Chats with the model. The request object can contain messages with images that are either
   * Uint8Arrays or base64 encoded strings. The images will be base64 encoded before sending the
   * request.
   * @param request {ChatRequest} - The request object.
   * @returns {Promise<ChatResponse | AbortableAsyncIterator<ChatResponse>>} - The response object or an
   * AbortableAsyncIterator that yields response messages.
   */
  async chat(request) {
    if (request.messages) {
      for (const message of request.messages) {
        if (message.images) {
          message.images = await Promise.all(
            message.images.map(this.encodeImage.bind(this))
          );
        }
      }
    }
    return this.processStreamableRequest("chat", request);
  }
  /**
   * Creates a new model from a stream of data.
   * @param request {CreateRequest} - The request object.
   * @returns {Promise<ProgressResponse | AbortableAsyncIterator<ProgressResponse>>} - The response object or a stream of progress responses.
   */
  async create(request) {
    return this.processStreamableRequest("create", {
      ...request
    });
  }
  /**
   * Pulls a model from the Ollama registry. The request object can contain a stream flag to indicate if the
   * response should be streamed.
   * @param request {PullRequest} - The request object.
   * @returns {Promise<ProgressResponse | AbortableAsyncIterator<ProgressResponse>>} - The response object or
   * an AbortableAsyncIterator that yields response messages.
   */
  async pull(request) {
    return this.processStreamableRequest("pull", {
      name: request.model,
      stream: request.stream,
      insecure: request.insecure
    });
  }
  /**
   * Pushes a model to the Ollama registry. The request object can contain a stream flag to indicate if the
   * response should be streamed.
   * @param request {PushRequest} - The request object.
   * @returns {Promise<ProgressResponse | AbortableAsyncIterator<ProgressResponse>>} - The response object or
   * an AbortableAsyncIterator that yields response messages.
   */
  async push(request) {
    return this.processStreamableRequest("push", {
      name: request.model,
      stream: request.stream,
      insecure: request.insecure
    });
  }
  /**
   * Deletes a model from the server. The request object should contain the name of the model to
   * delete.
   * @param request {DeleteRequest} - The request object.
   * @returns {Promise<StatusResponse>} - The response object.
   */
  async delete(request) {
    await del(
      this.fetch,
      `${this.config.host}/api/delete`,
      { name: request.model },
      { headers: this.config.headers }
    );
    return { status: "success" };
  }
  /**
   * Copies a model from one name to another. The request object should contain the name of the
   * model to copy and the new name.
   * @param request {CopyRequest} - The request object.
   * @returns {Promise<StatusResponse>} - The response object.
   */
  async copy(request) {
    await post(this.fetch, `${this.config.host}/api/copy`, { ...request }, {
      headers: this.config.headers
    });
    return { status: "success" };
  }
  /**
   * Lists the models on the server.
   * @returns {Promise<ListResponse>} - The response object.
   * @throws {Error} - If the response body is missing.
   */
  async list() {
    const response = await get(this.fetch, `${this.config.host}/api/tags`, {
      headers: this.config.headers
    });
    return await response.json();
  }
  /**
   * Shows the metadata of a model. The request object should contain the name of the model.
   * @param request {ShowRequest} - The request object.
   * @returns {Promise<ShowResponse>} - The response object.
   */
  async show(request) {
    const response = await post(this.fetch, `${this.config.host}/api/show`, {
      ...request
    }, {
      headers: this.config.headers
    });
    return await response.json();
  }
  /**
   * Embeds text input into vectors.
   * @param request {EmbedRequest} - The request object.
   * @returns {Promise<EmbedResponse>} - The response object.
   */
  async embed(request) {
    const response = await post(this.fetch, `${this.config.host}/api/embed`, {
      ...request
    }, {
      headers: this.config.headers
    });
    return await response.json();
  }
  /**
   * Embeds a text prompt into a vector.
   * @param request {EmbeddingsRequest} - The request object.
   * @returns {Promise<EmbeddingsResponse>} - The response object.
   */
  async embeddings(request) {
    const response = await post(this.fetch, `${this.config.host}/api/embeddings`, {
      ...request
    }, {
      headers: this.config.headers
    });
    return await response.json();
  }
  /**
   * Lists the running models on the server
   * @returns {Promise<ListResponse>} - The response object.
   * @throws {Error} - If the response body is missing.
   */
  async ps() {
    const response = await get(this.fetch, `${this.config.host}/api/ps`, {
      headers: this.config.headers
    });
    return await response.json();
  }
  /**
   * Returns the Ollama server version.
   * @returns {Promise<VersionResponse>} - The server version object.
   */
  async version() {
    const response = await get(this.fetch, `${this.config.host}/api/version`, {
      headers: this.config.headers
    });
    return await response.json();
  }
  /**
   * Performs web search using the Ollama web search API
   * @param request {WebSearchRequest} - The search request containing query and options
   * @returns {Promise<WebSearchResponse>} - The search results
   * @throws {Error} - If the request is invalid or the server returns an error
   */
  async webSearch(request) {
    if (!request.query || request.query.length === 0) {
      throw new Error("Query is required");
    }
    const response = await post(this.fetch, `https://ollama.com/api/web_search`, { ...request }, {
      headers: this.config.headers
    });
    return await response.json();
  }
  /**
   * Fetches a single page using the Ollama web fetch API
   * @param request {WebFetchRequest} - The fetch request containing a URL
   * @returns {Promise<WebFetchResponse>} - The fetch result
   * @throws {Error} - If the request is invalid or the server returns an error
   */
  async webFetch(request) {
    if (!request.url || request.url.length === 0) {
      throw new Error("URL is required");
    }
    const response = await post(this.fetch, `https://ollama.com/api/web_fetch`, { ...request }, { headers: this.config.headers });
    return await response.json();
  }
};
var browser = new Ollama$1();

// node_modules/ollama/dist/index.mjs
var Ollama2 = class extends Ollama$1 {
  async encodeImage(image) {
    if (typeof image !== "string") {
      return Buffer.from(image).toString("base64");
    }
    try {
      if (import_node_fs.default.existsSync(image)) {
        const fileBuffer = await import_node_fs.promises.readFile((0, import_node_path.resolve)(image));
        return Buffer.from(fileBuffer).toString("base64");
      }
    } catch {
    }
    return image;
  }
  /**
   * checks if a file exists
   * @param path {string} - The path to the file
   * @private @internal
   * @returns {Promise<boolean>} - Whether the file exists or not
   */
  async fileExists(path) {
    try {
      await import_node_fs.promises.access(path);
      return true;
    } catch {
      return false;
    }
  }
  async create(request) {
    if (request.from && await this.fileExists((0, import_node_path.resolve)(request.from))) {
      throw Error("Creating with a local path is not currently supported from ollama-js");
    }
    if (request.stream) {
      return super.create(request);
    } else {
      return super.create(request);
    }
  }
};
var index = new Ollama2();

// node_modules/@tanstack/ai-ollama/dist/esm/utils/client.js
function createOllamaClient(config = {}) {
  return new Ollama2({
    host: config.host || "http://localhost:11434",
    headers: config.headers
  });
}
function getOllamaHostFromEnv() {
  const env = typeof globalThis !== "undefined" && globalThis.window ? globalThis.window.env : typeof process !== "undefined" ? process.env : void 0;
  return env?.OLLAMA_HOST || "http://localhost:11434";
}
function generateId(prefix = "msg") {
  return `${prefix}-${Date.now()}-${Math.random().toString(36).substring(7)}`;
}
function estimateTokens(text) {
  return Math.ceil(text.length / 4);
}

// node_modules/@tanstack/ai-ollama/dist/esm/adapters/text.js
var OllamaTextAdapter = class extends BaseTextAdapter {
  constructor(hostOrClientOrConfig, model) {
    super({}, model);
    this.kind = "text";
    this.name = "ollama";
    if (typeof hostOrClientOrConfig === "string" || hostOrClientOrConfig === void 0) {
      this.client = createOllamaClient({ host: hostOrClientOrConfig });
    } else if ("chat" in hostOrClientOrConfig) {
      this.client = hostOrClientOrConfig;
    } else {
      this.client = createOllamaClient(hostOrClientOrConfig);
    }
  }
  async *chatStream(options) {
    const mappedOptions = this.mapCommonOptionsToOllama(options);
    const response = await this.client.chat({
      ...mappedOptions,
      stream: true
    });
    yield* this.processOllamaStreamChunks(response);
  }
  /**
   * Generate structured output using Ollama's JSON format option.
   * Uses format: 'json' with the schema to ensure structured output.
   * The outputSchema is already JSON Schema (converted in the ai layer).
   */
  async structuredOutput(options) {
    const { chatOptions, outputSchema } = options;
    const mappedOptions = this.mapCommonOptionsToOllama(chatOptions);
    try {
      const response = await this.client.chat({
        ...mappedOptions,
        stream: false,
        format: outputSchema
      });
      const rawText = response.message.content;
      let parsed;
      try {
        parsed = JSON.parse(rawText);
      } catch {
        throw new Error(
          `Failed to parse structured output as JSON. Content: ${rawText.slice(0, 200)}${rawText.length > 200 ? "..." : ""}`
        );
      }
      return {
        data: parsed,
        rawText
      };
    } catch (error) {
      const err = error;
      throw new Error(
        `Structured output generation failed: ${err.message || "Unknown error occurred"}`
      );
    }
  }
  async *processOllamaStreamChunks(stream) {
    let accumulatedContent = "";
    const timestamp = Date.now();
    let accumulatedReasoning = "";
    const toolCallsEmitted = /* @__PURE__ */ new Set();
    const runId = generateId("run");
    const messageId = generateId("msg");
    let stepId = null;
    let hasEmittedRunStarted = false;
    let hasEmittedTextMessageStart = false;
    let hasEmittedStepStarted = false;
    for await (const chunk of stream) {
      if (!hasEmittedRunStarted) {
        hasEmittedRunStarted = true;
        yield {
          type: "RUN_STARTED",
          runId,
          model: chunk.model,
          timestamp
        };
      }
      const handleToolCall = (toolCall) => {
        const actualToolCall = toolCall;
        const toolCallId = actualToolCall.id || `${actualToolCall.function.name}_${Date.now()}`;
        const events = [];
        if (!toolCallsEmitted.has(toolCallId)) {
          toolCallsEmitted.add(toolCallId);
          events.push({
            type: "TOOL_CALL_START",
            toolCallId,
            toolName: actualToolCall.function.name || "",
            model: chunk.model,
            timestamp,
            index: actualToolCall.function.index
          });
        }
        let parsedInput = {};
        const argsStr = typeof actualToolCall.function.arguments === "string" ? actualToolCall.function.arguments : JSON.stringify(actualToolCall.function.arguments);
        try {
          parsedInput = JSON.parse(argsStr);
        } catch {
          parsedInput = actualToolCall.function.arguments;
        }
        if (argsStr) {
          events.push({
            type: "TOOL_CALL_ARGS",
            toolCallId,
            model: chunk.model,
            timestamp,
            delta: argsStr,
            args: argsStr
          });
        }
        events.push({
          type: "TOOL_CALL_END",
          toolCallId,
          toolName: actualToolCall.function.name || "",
          model: chunk.model,
          timestamp,
          input: parsedInput
        });
        return events;
      };
      if (chunk.done) {
        if (chunk.message.tool_calls && chunk.message.tool_calls.length > 0) {
          for (const toolCall of chunk.message.tool_calls) {
            const events = handleToolCall(toolCall);
            for (const event of events) {
              yield event;
            }
          }
        }
        if (hasEmittedTextMessageStart) {
          yield {
            type: "TEXT_MESSAGE_END",
            messageId,
            model: chunk.model,
            timestamp
          };
        }
        yield {
          type: "RUN_FINISHED",
          runId,
          model: chunk.model,
          timestamp,
          finishReason: toolCallsEmitted.size > 0 ? "tool_calls" : "stop",
          usage: {
            promptTokens: chunk.prompt_eval_count || 0,
            completionTokens: chunk.eval_count || 0,
            totalTokens: (chunk.prompt_eval_count || 0) + (chunk.eval_count || 0)
          }
        };
        continue;
      }
      if (chunk.message.content) {
        if (!hasEmittedTextMessageStart) {
          hasEmittedTextMessageStart = true;
          yield {
            type: "TEXT_MESSAGE_START",
            messageId,
            model: chunk.model,
            timestamp,
            role: "assistant"
          };
        }
        accumulatedContent += chunk.message.content;
        yield {
          type: "TEXT_MESSAGE_CONTENT",
          messageId,
          model: chunk.model,
          timestamp,
          delta: chunk.message.content,
          content: accumulatedContent
        };
      }
      if (chunk.message.tool_calls && chunk.message.tool_calls.length > 0) {
        for (const toolCall of chunk.message.tool_calls) {
          const events = handleToolCall(toolCall);
          for (const event of events) {
            yield event;
          }
        }
      }
      if (chunk.message.thinking) {
        if (!hasEmittedStepStarted) {
          hasEmittedStepStarted = true;
          stepId = generateId("step");
          yield {
            type: "STEP_STARTED",
            stepId,
            model: chunk.model,
            timestamp,
            stepType: "thinking"
          };
        }
        accumulatedReasoning += chunk.message.thinking;
        yield {
          type: "STEP_FINISHED",
          stepId: stepId || generateId("step"),
          model: chunk.model,
          timestamp,
          delta: chunk.message.thinking,
          content: accumulatedReasoning
        };
      }
    }
  }
  convertToolsToOllamaFormat(tools) {
    if (!tools || tools.length === 0) {
      return void 0;
    }
    return tools.map((tool) => ({
      type: "function",
      function: {
        name: tool.name,
        description: tool.description,
        parameters: tool.inputSchema ?? {
          type: "object",
          properties: {},
          required: []
        }
      }
    }));
  }
  formatMessages(messages) {
    return messages.map((msg) => {
      let textContent = "";
      const images = [];
      if (Array.isArray(msg.content)) {
        for (const part of msg.content) {
          if (part.type === "text") {
            textContent += part.content;
          } else if (part.type === "image") {
            if (part.source.type === "data") {
              images.push(part.source.value);
            } else {
              images.push(part.source.value);
            }
          }
        }
      } else {
        textContent = msg.content || "";
      }
      const hasToolCallId = msg.role === "tool" && msg.toolCallId;
      return {
        role: hasToolCallId ? "tool" : msg.role,
        content: hasToolCallId ? typeof msg.content === "string" ? msg.content : JSON.stringify(msg.content) : textContent,
        ...images.length > 0 ? { images } : {},
        ...msg.role === "assistant" && msg.toolCalls && msg.toolCalls.length > 0 ? {
          tool_calls: msg.toolCalls.map((toolCall) => {
            let parsedArguments = {};
            if (typeof toolCall.function.arguments === "string") {
              try {
                parsedArguments = JSON.parse(
                  toolCall.function.arguments
                );
              } catch {
                parsedArguments = {};
              }
            } else {
              parsedArguments = toolCall.function.arguments;
            }
            return {
              id: toolCall.id,
              type: toolCall.type,
              function: {
                name: toolCall.function.name,
                arguments: parsedArguments
              }
            };
          })
        } : {}
      };
    });
  }
  mapCommonOptionsToOllama(options) {
    const model = options.model;
    const modelOptions = options.modelOptions;
    const ollamaOptions = {
      temperature: options.temperature,
      top_p: options.topP,
      num_predict: options.maxTokens,
      ...modelOptions
    };
    return {
      model,
      options: ollamaOptions,
      messages: this.formatMessages(options.messages),
      tools: this.convertToolsToOllamaFormat(options.tools)
    };
  }
};
function createOllamaChat(model, hostOrConfig) {
  return new OllamaTextAdapter(hostOrConfig, model);
}
function ollamaText(model) {
  const host = getOllamaHostFromEnv();
  return new OllamaTextAdapter(host, model);
}

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-athene.js
var ATHENE_V2_LATEST = {
  name: "athene-v2:latest"
};
var ATHENE_V2_72b = {
  name: "athene-v2:72b"
};
var ATHENE_MODELS = [
  ATHENE_V2_LATEST.name,
  ATHENE_V2_72b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-aya.js
var AYA_LATEST = {
  name: "aya:latest"
};
var AYA_8b = {
  name: "aya:8b"
};
var AYA_35b = {
  name: "aya:35b"
};
var AYA_MODELS = [AYA_LATEST.name, AYA_8b.name, AYA_35b.name];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-codegemma.js
var CODEGEMMA_LATEST = {
  name: "codegemma:latest"
};
var CODEGEMMA_2b = {
  name: "codegemma:2b"
};
var CODEGEMMA_7b = {
  name: "codegemma:7b"
};
var CODEGEMMA_MODELS = [
  CODEGEMMA_LATEST.name,
  CODEGEMMA_2b.name,
  CODEGEMMA_7b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-codellama.js
var CODELLAMA_LATEST = {
  name: "codellama:latest"
};
var CODELLAMA_7b = {
  name: "codellama:7b"
};
var CODELLAMA_13b = {
  name: "codellama:13b"
};
var CODELLAMA_34b = {
  name: "codellama:34b"
};
var CODELLAMA_70b = {
  name: "codellama:70b"
};
var CODELLAMA_MODELS = [
  CODELLAMA_LATEST.name,
  CODELLAMA_7b.name,
  CODELLAMA_13b.name,
  CODELLAMA_34b.name,
  CODELLAMA_70b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-command-r.js
var COMMAND_R_LATEST = {
  name: "command-r:latest"
};
var COMMAND_R_35b = {
  name: "command-r:35b"
};
var COMMAND_R_MODELS = [
  COMMAND_R_LATEST.name,
  COMMAND_R_35b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-command-r-plus.js
var COMMAND_R_PLUS_LATEST = {
  name: "command-r-plus:latest"
};
var COMMAND_R_PLUS_104b = {
  name: "command-r-plus:104b"
};
var COMMAND_R_PLUS_MODELS = [
  COMMAND_R_PLUS_LATEST.name,
  COMMAND_R_PLUS_104b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-command-r7b.js
var COMMAND_R_7b_LATEST = {
  name: "command-r7b:latest"
};
var COMMAND_R_7b_7b = {
  name: "command-r7b:7b"
};
var COMMAND_R_7b_MODELS = [
  COMMAND_R_7b_LATEST.name,
  COMMAND_R_7b_7b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-deepseek-coder-v2.js
var DEEPSEEK_CODER_V2_LATEST = {
  name: "deepseek-coder-v2:latest"
};
var DEEPSEEK_CODER_V2_16b = {
  name: "deepseek-coder-v2:16b"
};
var DEEPSEEK_CODER_V2_236b = {
  name: "deepseek-coder-v2:236b"
};
var DEEPSEEK_CODER_V2_MODELS = [
  DEEPSEEK_CODER_V2_LATEST.name,
  DEEPSEEK_CODER_V2_16b.name,
  DEEPSEEK_CODER_V2_236b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-deepseek-ocr.js
var DEEPSEEK_OCR_LATEST = {
  name: "deepseek-ocr:latest"
};
var DEEPSEEK_OCR_3b = {
  name: "deepseek-ocr:3b"
};
var DEEPSEEK_OCR_MODELS = [
  DEEPSEEK_OCR_LATEST.name,
  DEEPSEEK_OCR_3b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-deepseek-r1.js
var DEEPSEEK_R1_LATEST = {
  name: "deepseek-r1:latest"
};
var DEEPSEEK_R1_1_5b = {
  name: "deepseek-r1:1.5b"
};
var DEEPSEEK_R1_7b = {
  name: "deepseek-r1:7b"
};
var DEEPSEEK_R1_8b = {
  name: "deepseek-r1:8b"
};
var DEEPSEEK_R1_32b = {
  name: "deepseek-r1:32b"
};
var DEEPSEEK_R1_70b = {
  name: "deepseek-r1:70b"
};
var DEEPSEEK_R1_671b = {
  name: "deepseek-r1:671b"
};
var DEEPSEEK_R1_MODELS = [
  DEEPSEEK_R1_LATEST.name,
  DEEPSEEK_R1_1_5b.name,
  DEEPSEEK_R1_7b.name,
  DEEPSEEK_R1_8b.name,
  DEEPSEEK_R1_32b.name,
  DEEPSEEK_R1_70b.name,
  DEEPSEEK_R1_671b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-deepseek-v3.1.js
var DEEPSEEK_V3_1_LATEST = {
  name: "deepseek-v3.1:latest"
};
var DEEPSEEK_V3_1_671b = {
  name: "deepseek-v3.1:671b"
};
var DEEPSEEK_V3_1_671b_cloud = {
  name: "deepseek-v3.1:671b-cloud"
};
var DEEPSEEK_V3_1_MODELS = [
  DEEPSEEK_V3_1_LATEST.name,
  DEEPSEEK_V3_1_671b.name,
  DEEPSEEK_V3_1_671b_cloud.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-devstral.js
var DEVSTRAL_LATEST = {
  name: "devstral:latest"
};
var DEVSTRAL_24b = {
  name: "devstral:24b"
};
var DEVSTRAL_MODELS = [
  DEVSTRAL_LATEST.name,
  DEVSTRAL_24b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-dolphin3.js
var DOLPHIN3_LATEST = {
  name: "dolphin3:latest"
};
var DOLPHIN3_8b = {
  name: "dolphin3:8b"
};
var DOLPHIN3_MODELS = [DOLPHIN3_LATEST.name, DOLPHIN3_8b.name];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-exaone3.5.js
var EXAONE3_5_LATEST = {
  name: "exaone3.5:latest"
};
var EXAONE3_5_2_4b = {
  name: "exaone3.5:2.4b"
};
var EXAONE3_5_7_1b = {
  name: "exaone3.5:7.8b"
};
var EXAONE3_5_32b = {
  name: "exaone3.5:32b"
};
var EXAONE3_5MODELS = [
  EXAONE3_5_LATEST.name,
  EXAONE3_5_2_4b.name,
  EXAONE3_5_7_1b.name,
  EXAONE3_5_32b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-falcon2.js
var FALCON2_LATEST = {
  name: "falcon2:latest"
};
var FALCON2_11b = {
  name: "falcon2:11b"
};
var FALCON2_MODELS = [FALCON2_LATEST.name, FALCON2_11b.name];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-falcon3.js
var FALCON3_LATEST = {
  name: "falcon3:latest"
};
var FALCON3_1b = {
  name: "falcon3:1b"
};
var FALCON3_3b = {
  name: "falcon3:3b"
};
var FALCON3_7b = {
  name: "falcon3:7b"
};
var FALCON3_10b = {
  name: "falcon3:10b"
};
var FALCON3_MODELS = [
  FALCON3_LATEST.name,
  FALCON3_1b.name,
  FALCON3_3b.name,
  FALCON3_7b.name,
  FALCON3_10b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-firefunction-v2.js
var FIREFUNCTION_V2_LATEST = {
  name: "firefunction-v2:latest"
};
var FIREFUNCTION_V2_70b = {
  name: "firefunction-v2:70b"
};
var FIREFUNCTION_V2_MODELS = [
  FIREFUNCTION_V2_LATEST.name,
  FIREFUNCTION_V2_70b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-gemma.js
var GEMMA_LATEST = {
  name: "gemma:latest"
};
var GEMMA_2b = {
  name: "gemma:2b"
};
var GEMMA_7b = {
  name: "gemma:7b"
};
var GEMMA_MODELS = [
  GEMMA_LATEST.name,
  GEMMA_2b.name,
  GEMMA_7b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-gemma2.js
var GEMMA2_LATEST = {
  name: "gemma2:latest"
};
var GEMMA2_2b = {
  name: "gemma2:2b"
};
var GEMMA2_9b = {
  name: "gemma2:9b"
};
var GEMMA2_27b = {
  name: "gemma2:27b"
};
var GEMMA2_MODELS = [
  GEMMA2_LATEST.name,
  GEMMA2_2b.name,
  GEMMA2_9b.name,
  GEMMA2_27b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-gemma3.js
var GEMMA3_LATEST = {
  name: "gemma3:latest"
};
var GEMMA3_270m = {
  name: "gemma3:270m"
};
var GEMMA3_1b = {
  name: "gemma3:1b"
};
var GEMMA3_4b = {
  name: "gemma3:4b"
};
var GEMMA3_12b = {
  name: "gemma3:12b"
};
var GEMMA3_27b = {
  name: "gemma3:27b"
};
var GEMMA3_MODELS = [
  GEMMA3_LATEST.name,
  GEMMA3_270m.name,
  GEMMA3_1b.name,
  GEMMA3_4b.name,
  GEMMA3_12b.name,
  GEMMA3_27b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-gpt-oss.js
var OPT_OSS_LATEST = {
  name: "gpt-oss:latest"
};
var OPT_OSS_20b = {
  name: "gpt-oss:20b"
};
var OPT_OSS_120b = {
  name: "gpt-oss:120b"
};
var GPT_OSS_MODELS = [
  OPT_OSS_LATEST.name,
  OPT_OSS_20b.name,
  OPT_OSS_120b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-granite3-dense.js
var GRANITE3_DENSE_LATEST = {
  name: "granite3-dense:latest"
};
var GRANITE3_DENSE_2b = {
  name: "granite3-dense:2b"
};
var GRANITE3_DENSE_8b = {
  name: "granite3-dense:8b"
};
var GRANITE3_DENSE_MODELS = [
  GRANITE3_DENSE_LATEST.name,
  GRANITE3_DENSE_2b.name,
  GRANITE3_DENSE_8b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-granite3-guardian.js
var GRANITE3_GUARDIAN_LATEST = {
  name: "granite3-guardian:latest"
};
var GRANITE3_GUARDIAN_2b = {
  name: "granite3-guardian:2b"
};
var GRANITE3_GUARDIAN_8b = {
  name: "granite3-guardian:8b"
};
var GRANITE3_GUARDIAN_MODELS = [
  GRANITE3_GUARDIAN_LATEST.name,
  GRANITE3_GUARDIAN_2b.name,
  GRANITE3_GUARDIAN_8b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-granite3-moe.js
var GRANITE3_MOE_LATEST = {
  name: "granite3-moe:latest"
};
var GRANITE3_MOE_1b = {
  name: "granite3-moe:1b"
};
var GRANITE3_MOE_3b = {
  name: "granite3-moe:3b"
};
var GRANITE3_MOE_MODELS = [
  GRANITE3_MOE_LATEST.name,
  GRANITE3_MOE_1b.name,
  GRANITE3_MOE_3b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-granite3.1-dense.js
var GRANITE3_1_DENSE_LATEST = {
  name: "granite3.1-dense:latest"
};
var GRANITE3_1_DENSE_2b = {
  name: "granite3.1-dense:2b"
};
var GRANITE3_1_DENSE_8b = {
  name: "granite3.1-dense:8b"
};
var GRANITE3_1_DENSE_MODELS = [
  GRANITE3_1_DENSE_LATEST.name,
  GRANITE3_1_DENSE_2b.name,
  GRANITE3_1_DENSE_8b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-granite3.1-moe.js
var GRANITE3_1_MOE_LATEST = {
  name: "granite3.1-moe:latest"
};
var GRANITE3_1_MOE_1b = {
  name: "granite3.1-moe:1b"
};
var GRANITE3_1_MOE_3b = {
  name: "granite3.1-moe:3b"
};
var GRANITE3_1_MOE_MODELS = [
  GRANITE3_1_MOE_LATEST.name,
  GRANITE3_1_MOE_1b.name,
  GRANITE3_1_MOE_3b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llama-guard3.js
var LLAMA_GUARD3_LATEST = {
  name: "llama-guard3:latest"
};
var LLAMA_GUARD3_1b = {
  name: "llama-guard3:1b"
};
var LLAMA_GUARD3_8b = {
  name: "llama-guard3:8b"
};
var LLAMA_GUARD3_MODELS = [
  LLAMA_GUARD3_LATEST.name,
  LLAMA_GUARD3_1b.name,
  LLAMA_GUARD3_8b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llama2.js
var LLAMA2_LATEST = {
  name: "llama2:latest"
};
var LLAMA2_7b = {
  name: "llama2:7b"
};
var LLAMA2_13b = {
  name: "llama2:13b"
};
var LLAMA2_70b = {
  name: "llama2:70b"
};
var LLAMA2_MODELS = [
  LLAMA2_LATEST.name,
  LLAMA2_7b.name,
  LLAMA2_13b.name,
  LLAMA2_70b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llama3.js
var LLAMA3_LATEST = {
  name: "llama3:latest"
};
var LLAMA3_8b = {
  name: "llama3:8b"
};
var LLAMA3_70b = {
  name: "llama3:70b"
};
var LLAMA3_MODELS = [
  LLAMA3_LATEST.name,
  LLAMA3_8b.name,
  LLAMA3_70b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llama3-chatqa.js
var LLAMA3_CHATQA_LATEST = {
  name: "llama3-chatqa:latest"
};
var LLAMA3_CHATQA_8b = {
  name: "llama3-chatqa:8b"
};
var LLAMA3_CHATQA_70b = {
  name: "llama3-chatqa:70b"
};
var LLAMA3_CHATQA_MODELS = [
  LLAMA3_CHATQA_LATEST.name,
  LLAMA3_CHATQA_8b.name,
  LLAMA3_CHATQA_70b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llama3-gradient.js
var LLAMA3_GRADIENT_LATEST = {
  name: "llama3-gradient:latest"
};
var LLAMA3_GRADIENT_8b = {
  name: "llama3-gradient:8b"
};
var LLAMA3_GRADIENT_70b = {
  name: "llama3-gradient:70b"
};
var LLAMA3_GRADIENT_MODELS = [
  LLAMA3_GRADIENT_LATEST.name,
  LLAMA3_GRADIENT_8b.name,
  LLAMA3_GRADIENT_70b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llama3.1.js
var LLAMA3_1_LATEST = {
  name: "llama3.1:latest"
};
var LLAMA3_1_8b = {
  name: "llama3.1:8b"
};
var LLAMA3_1_70b = {
  name: "llama3.1:70b"
};
var LLAMA3_1_405b = {
  name: "llama3.1:405b"
};
var LLAMA3_1_MODELS = [
  LLAMA3_1_LATEST.name,
  LLAMA3_1_8b.name,
  LLAMA3_1_70b.name,
  LLAMA3_1_405b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llama3.2.js
var LLAMA3_2_LATEST = {
  name: "llama3.2:latest"
};
var LLAMA3_2_1b = {
  name: "llama3.2:1b"
};
var LLAMA3_2_3b = {
  name: "llama3.2:3b"
};
var LLAMA3_2_MODELS = [
  LLAMA3_2_LATEST.name,
  LLAMA3_2_1b.name,
  LLAMA3_2_3b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llama3.2-vision.js
var LLAMA3_2_VISION_LATEST = {
  name: "llama3.2:latest"
};
var LLAMA3_2_VISION_11b = {
  name: "llama3.2:11b"
};
var LLAMA3_2_VISION_90b = {
  name: "llama3.2:90b"
};
var LLAMA3_2_VISION_MODELS = [
  LLAMA3_2_VISION_LATEST.name,
  LLAMA3_2_VISION_11b.name,
  LLAMA3_2_VISION_90b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llama3.3.js
var LLAMA3_3_LATEST = {
  name: "llama3.3:latest"
};
var LLAMA3_3_70b = {
  name: "llama3.3:70b"
};
var LLAMA3_3_MODELS = [
  LLAMA3_3_LATEST.name,
  LLAMA3_3_70b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llama4.js
var LLAMA4_LATEST = {
  name: "llama4:latest"
};
var LLAMA4_16X17b = {
  name: "llama4:16x17b"
};
var LLAMA4_128X17b = {
  name: "llama4:128x17b"
};
var LLAMA4_MODELS = [
  LLAMA4_LATEST.name,
  LLAMA4_16X17b.name,
  LLAMA4_128X17b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llava.js
var LLAVA_LATEST = {
  name: "llava:latest"
};
var LLAVA_7b = {
  name: "llava:7b"
};
var LLAVA_13b = {
  name: "llava:13b"
};
var LLAVA_34b = {
  name: "llava:34b"
};
var LLAVA_MODELS = [
  LLAVA_LATEST.name,
  LLAVA_7b.name,
  LLAVA_13b.name,
  LLAVA_34b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llava-llama3.js
var LLAVA_LLAMA3_LATEST = {
  name: "llava-llama3:latest"
};
var LLAVA_LLAMA3_8b = {
  name: "llava-llama3:8b"
};
var LLAVA_LLAMA3_MODELS = [
  LLAVA_LLAMA3_LATEST.name,
  LLAVA_LLAMA3_8b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-llava-phi3.js
var LLAVA_PHI3_LATEST = {
  name: "llava-phi3:latest"
};
var LLAVA_PHI3_8b = {
  name: "llava-phi3:8b"
};
var LLAVA_PHI3_MODELS = [
  LLAVA_PHI3_LATEST.name,
  LLAVA_PHI3_8b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-marco-o1.js
var MARCO_O1_LATEST = {
  name: "marco-o1:latest"
};
var MARCO_O1_7b = {
  name: "marco-o1:7b"
};
var MARCO_O1_MODELS = [MARCO_O1_LATEST.name, MARCO_O1_7b.name];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-mistral.js
var MISTRAL_LATEST = {
  name: "mistral:latest"
};
var MISTRAL_7b = {
  name: "mistral:7b"
};
var MISTRAL_MODELS = [MISTRAL_LATEST.name, MISTRAL_7b.name];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-mistral-large.js
var MISTRAL_LARGE_LATEST = {
  name: "mistral-large:latest"
};
var MISTRAL_LARGE_123b = {
  name: "mistral-large:123b"
};
var MISTRAL_LARGE_MODELS = [
  MISTRAL_LARGE_LATEST.name,
  MISTRAL_LARGE_123b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-mistral-nemo.js
var MISTRAL_NEMO_LATEST = {
  name: "mistral-nemo:latest"
};
var MISTRAL_NEMO_12b = {
  name: "mistral-nemo:12b"
};
var MISTRAL_NEMO_MODELS = [
  MISTRAL_NEMO_LATEST.name,
  MISTRAL_NEMO_12b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-mistral-small.js
var MISTRAL_SMALL_LATEST = {
  name: "mistral-small:latest"
};
var MISTRAL_SMALL_22b = {
  name: "mistral-small:22b"
};
var MISTRAL_SMALL_24b = {
  name: "mistral-small:24b"
};
var MISTRAL_SMALL_MODELS = [
  MISTRAL_SMALL_LATEST.name,
  MISTRAL_SMALL_22b.name,
  MISTRAL_SMALL_24b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-mixtral.js
var MIXTRAL_LATEST = {
  name: "mixtral:latest"
};
var MIXTRAL_8X7b = {
  name: "mixtral:8x7b"
};
var MIXTRAL_8X22b = {
  name: "mixtral:8x22b"
};
var MIXTRAL_MODELS = [
  MIXTRAL_LATEST.name,
  MIXTRAL_8X7b.name,
  MIXTRAL_8X22b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-moondream.js
var MOONDREAM_LATEST = {
  name: "moondream:latest"
};
var MOONDREAM_1_8b = {
  name: "moondream:1.8b"
};
var MOONDREAM_MODELS = [
  MOONDREAM_LATEST.name,
  MOONDREAM_1_8b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-nemotron.js
var NEMOTRON_LATEST = {
  name: "nemotron:latest"
};
var NEMOTRON_70b = {
  name: "nemotron:70b"
};
var NEMOTRON_MODELS = [
  NEMOTRON_LATEST.name,
  NEMOTRON_70b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-nemotron-mini.js
var NEMOTRON_MINI_LATEST = {
  name: "nemotron-mini:latest"
};
var NEMOTRON_MINI_4b = {
  name: "nemotron-mini:4b"
};
var NEMOTRON_MINI_MODELS = [
  NEMOTRON_MINI_LATEST.name,
  NEMOTRON_MINI_4b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-olmo2.js
var OLMO2_LATEST = {
  name: "olmo2:latest"
};
var OLMO2_7b = {
  name: "olmo2:7b"
};
var OLMO2_13b = {
  name: "olmo2:13b"
};
var OLMO2_MODELS = [
  OLMO2_LATEST.name,
  OLMO2_7b.name,
  OLMO2_13b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-opencoder.js
var OPENCODER_LATEST = {
  name: "opencoder:latest"
};
var OPENCODER_1_5b = {
  name: "opencoder:1.5b"
};
var OPENCODER_8b = {
  name: "opencoder:8b"
};
var OPENCODER_MODELS = [
  OPENCODER_LATEST.name,
  OPENCODER_1_5b.name,
  OPENCODER_8b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-openhermes.js
var OPENHERMES_LATEST = {
  name: "openhermes:latest"
};
var OPENHERMES_V2 = {
  name: "openhermes:v2"
};
var OPENHERMES_V2_5 = {
  name: "openhermes:v2.5"
};
var OPENHERMES_MODELS = [
  OPENHERMES_LATEST.name,
  OPENHERMES_V2.name,
  OPENHERMES_V2_5.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-phi3.js
var PHI3_LATEST = {
  name: "phi3:latest"
};
var PHI3_3_8b = {
  name: "phi3:8b"
};
var PHI3_14b = {
  name: "phi3:14b"
};
var PHI3_MODELS = [
  PHI3_LATEST.name,
  PHI3_3_8b.name,
  PHI3_14b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-phi4.js
var PHI4_LATEST = {
  name: "phi4:latest"
};
var PHI4_14b = {
  name: "phi4:14b"
};
var PHI4_MODELS = [PHI4_LATEST.name, PHI4_14b.name];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-qwen.js
var QWEN_LATEST = {
  name: "qwen:latest"
};
var QWEN_0_5b = {
  name: "qwen:0.5b"
};
var QWEN_1_8b = {
  name: "qwen:1.8b"
};
var QWEN_4b = {
  name: "qwen:4b"
};
var QWEN_7b = {
  name: "qwen:7b"
};
var QWEN_14b = {
  name: "qwen:14b"
};
var QWEN_32b = {
  name: "qwen:32b"
};
var QWEN_72b = {
  name: "qwen:72b"
};
var QWEN_110b = {
  name: "qwen:110b"
};
var QWEN_MODELS = [
  QWEN_LATEST.name,
  QWEN_0_5b.name,
  QWEN_1_8b.name,
  QWEN_4b.name,
  QWEN_7b.name,
  QWEN_14b.name,
  QWEN_32b.name,
  QWEN_72b.name,
  QWEN_110b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-qwen2.js
var QWEN2_LATEST = {
  name: "qwen2:latest"
};
var QWEN2_0_5b = {
  name: "qwen2:0.5b"
};
var QWEN2_1_5b = {
  name: "qwen2:1.5b"
};
var QWEN2_7b = {
  name: "qwen2:7b"
};
var QWEN2_72b = {
  name: "qwen2:72b"
};
var QWEN2_MODELS = [
  QWEN2_LATEST.name,
  QWEN2_0_5b.name,
  QWEN2_1_5b.name,
  QWEN2_7b.name,
  QWEN2_72b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-qwen2.5.js
var QWEN2_5_LATEST = {
  name: "qwen2.5:latest"
};
var QWEN2_5_0_5b = {
  name: "qwen2.5:0.5b"
};
var QWEN2_5_1_5b = {
  name: "qwen2.5:1.5b"
};
var QWEN2_5_3b = {
  name: "qwen2.5:3b"
};
var QWEN2_5_7b = {
  name: "qwen2.5:7b"
};
var QWEN2_5_32b = {
  name: "qwen2.5:32b"
};
var QWEN2_5_72b = {
  name: "qwen2.5:72b"
};
var QWEN2_5_MODELS = [
  QWEN2_5_LATEST.name,
  QWEN2_5_0_5b.name,
  QWEN2_5_1_5b.name,
  QWEN2_5_3b.name,
  QWEN2_5_7b.name,
  QWEN2_5_32b.name,
  QWEN2_5_72b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-qwen2.5-coder.js
var QWEN2_5_CODER_LATEST = {
  name: "qwen2.5-coder:latest"
};
var QWEN2_5_CODER_0_5b = {
  name: "qwen2.5-coder:0.5b"
};
var QWEN2_5_CODER_1_5b = {
  name: "qwen2.5-coder:1.5b"
};
var QWEN2_5_CODER_3b = {
  name: "qwen2.5-coder:3b"
};
var QWEN2_5_CODER_7b = {
  name: "qwen2.5-coder:7b"
};
var QWEN2_5_CODER_14b = {
  name: "qwen2.5-coder:14b"
};
var QWEN2_5_CODER_32b = {
  name: "qwen2.5-coder:32b"
};
var QWEN2_5_CODER_MODELS = [
  QWEN2_5_CODER_LATEST.name,
  QWEN2_5_CODER_0_5b.name,
  QWEN2_5_CODER_1_5b.name,
  QWEN2_5_CODER_3b.name,
  QWEN2_5_CODER_7b.name,
  QWEN2_5_CODER_14b.name,
  QWEN2_5_CODER_32b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-qwen3.js
var QWEN3_LATEST = {
  name: "qwen3:latest"
};
var QWEN3_0_6b = {
  name: "qwen3:0.6b"
};
var QWEN3_1_7b = {
  name: "qwen3:1.7b"
};
var QWEN3_4b = {
  name: "qwen3:4b"
};
var QWEN3_8b = {
  name: "qwen3:8b"
};
var QWEN3_14b = {
  name: "qwen3:14b"
};
var QWEN3_30b = {
  name: "qwen3:30b"
};
var QWEN3_32b = {
  name: "qwen3:32b"
};
var QWEN3_235b = {
  name: "qwen3:235b"
};
var QWEN3_MODELS = [
  QWEN3_LATEST.name,
  QWEN3_0_6b.name,
  QWEN3_1_7b.name,
  QWEN3_4b.name,
  QWEN3_8b.name,
  QWEN3_14b.name,
  QWEN3_30b.name,
  QWEN3_32b.name,
  QWEN3_235b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-qwq.js
var QWQ_LATEST = {
  name: "qwq:latest"
};
var QWQ_32b = {
  name: "qwq:32b"
};
var QWQ_MODELS = [QWQ_LATEST.name, QWQ_32b.name];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-sailor2.js
var SAILOR2_LATEST = {
  name: "sailor2:latest"
};
var SAILOR2_8b = {
  name: "sailor2:8b"
};
var SAILOR2_20b = {
  name: "sailor2:20b"
};
var SAILOR2_MODELS = [
  SAILOR2_LATEST.name,
  SAILOR2_8b.name,
  SAILOR2_20b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-shieldgemma.js
var SHIELDGEMMA_LATEST = {
  name: "shieldgemma:latest"
};
var SHIELDGEMMA_2b = {
  name: "shieldgemma:2b"
};
var SHIELDGEMMA_9b = {
  name: "shieldgemma:9b"
};
var SHIELDGEMMA_27b = {
  name: "shieldgemma:27b"
};
var SHIELDGEMMA_MODELS = [
  SHIELDGEMMA_LATEST.name,
  SHIELDGEMMA_2b.name,
  SHIELDGEMMA_9b.name,
  SHIELDGEMMA_27b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-smalltinker.js
var SMALLTINKER_LATEST = {
  name: "smalltinker:latest"
};
var SMALLTINKER_3b = {
  name: "smalltinker:3b"
};
var SMALLTINKER_MODELS = [
  SMALLTINKER_LATEST.name,
  SMALLTINKER_3b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-smollm.js
var SMOLLM_LATEST = {
  name: "smollm:latest"
};
var SMOLLM_135m = {
  name: "smollm:135m"
};
var SMOLLM_360m = {
  name: "smollm:360m"
};
var SMOLLM_1_7b = {
  name: "smollm:1.7b"
};
var SMOLLM_MODELS = [
  SMOLLM_LATEST.name,
  SMOLLM_135m.name,
  SMOLLM_360m.name,
  SMOLLM_1_7b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-tinyllama.js
var TINYLLAMA_LATEST = {
  name: "tinyllama:latest"
};
var TINYLLAMA_1_1b = {
  name: "tinyllama:1.1b"
};
var TINYLLAMA_MODELS = [
  TINYLLAMA_LATEST.name,
  TINYLLAMA_1_1b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/meta/model-meta-tulu3.js
var TULU3_LATEST = {
  name: "tulu3:latest"
};
var TULU3_8b = {
  name: "tulu3:8b"
};
var TULU3_70b = {
  name: "tulu3:70b"
};
var TULU3_MODELS = [
  TULU3_LATEST.name,
  TULU3_8b.name,
  TULU3_70b.name
];

// node_modules/@tanstack/ai-ollama/dist/esm/model-meta.js
var OLLAMA_TEXT_MODELS = [
  ...ATHENE_MODELS,
  ...AYA_MODELS,
  ...CODEGEMMA_MODELS,
  ...CODELLAMA_MODELS,
  ...COMMAND_R_PLUS_MODELS,
  ...COMMAND_R_MODELS,
  ...COMMAND_R_7b_MODELS,
  ...DEEPSEEK_CODER_V2_MODELS,
  ...DEEPSEEK_OCR_MODELS,
  ...DEEPSEEK_R1_MODELS,
  ...DEEPSEEK_V3_1_MODELS,
  ...DEVSTRAL_MODELS,
  ...DOLPHIN3_MODELS,
  ...EXAONE3_5MODELS,
  ...FALCON2_MODELS,
  ...FALCON3_MODELS,
  ...FIREFUNCTION_V2_MODELS,
  ...GEMMA_MODELS,
  ...GEMMA2_MODELS,
  ...GEMMA3_MODELS,
  ...GPT_OSS_MODELS,
  ...GRANITE3_DENSE_MODELS,
  ...GRANITE3_GUARDIAN_MODELS,
  ...GRANITE3_MOE_MODELS,
  ...GRANITE3_1_DENSE_MODELS,
  ...GRANITE3_1_MOE_MODELS,
  ...LLAMA_GUARD3_MODELS,
  ...LLAMA2_MODELS,
  ...LLAMA3_CHATQA_MODELS,
  ...LLAMA3_GRADIENT_MODELS,
  ...LLAMA3_1_MODELS,
  ...LLAMA3_2_MODELS,
  ...LLAMA3_2_VISION_MODELS,
  ...LLAMA3_3_MODELS,
  ...LLAMA3_MODELS,
  ...LLAMA4_MODELS,
  ...LLAVA_LLAMA3_MODELS,
  ...LLAVA_PHI3_MODELS,
  ...LLAVA_MODELS,
  ...MARCO_O1_MODELS,
  ...MISTRAL_LARGE_MODELS,
  ...MISTRAL_NEMO_MODELS,
  ...MISTRAL_SMALL_MODELS,
  ...MISTRAL_MODELS,
  ...MIXTRAL_MODELS,
  ...MOONDREAM_MODELS,
  ...NEMOTRON_MINI_MODELS,
  ...NEMOTRON_MODELS,
  ...OLMO2_MODELS,
  ...OPENCODER_MODELS,
  ...OPENHERMES_MODELS,
  ...PHI3_MODELS,
  ...PHI4_MODELS,
  ...QWEN_MODELS,
  ...QWEN2_5_CODER_MODELS,
  ...QWEN2_5_MODELS,
  ...QWEN2_MODELS,
  ...QWEN3_MODELS,
  ...QWQ_MODELS,
  ...SAILOR2_MODELS,
  ...SHIELDGEMMA_MODELS,
  ...SMALLTINKER_MODELS,
  ...SMOLLM_MODELS,
  ...TINYLLAMA_MODELS,
  ...TULU3_MODELS
];

// node_modules/@tanstack/ai-ollama/dist/esm/adapters/summarize.js
var OllamaSummarizeAdapter = class {
  constructor(hostOrClient, model, _options = {}) {
    this.kind = "summarize";
    this.name = "ollama";
    if (typeof hostOrClient === "string" || hostOrClient === void 0) {
      this.client = createOllamaClient({ host: hostOrClient });
    } else {
      this.client = hostOrClient;
    }
    this.model = model;
  }
  async summarize(options) {
    const model = options.model;
    const prompt = this.buildSummarizationPrompt(options);
    const response = await this.client.generate({
      model,
      prompt,
      options: {
        temperature: 0.3,
        num_predict: options.maxLength ?? 500
      },
      stream: false
    });
    const promptTokens = estimateTokens(prompt);
    const completionTokens = estimateTokens(response.response);
    return {
      id: generateId("sum"),
      model: response.model,
      summary: response.response,
      usage: {
        promptTokens,
        completionTokens,
        totalTokens: promptTokens + completionTokens
      }
    };
  }
  async *summarizeStream(options) {
    const model = options.model;
    const id = generateId("sum");
    const prompt = this.buildSummarizationPrompt(options);
    let accumulatedContent = "";
    const stream = await this.client.generate({
      model,
      prompt,
      options: {
        temperature: 0.3,
        num_predict: options.maxLength ?? 500
      },
      stream: true
    });
    for await (const chunk of stream) {
      if (chunk.response) {
        accumulatedContent += chunk.response;
        yield {
          type: "TEXT_MESSAGE_CONTENT",
          messageId: id,
          model: chunk.model,
          timestamp: Date.now(),
          delta: chunk.response,
          content: accumulatedContent
        };
      }
      if (chunk.done) {
        const promptTokens = estimateTokens(prompt);
        const completionTokens = estimateTokens(accumulatedContent);
        yield {
          type: "RUN_FINISHED",
          runId: id,
          model: chunk.model,
          timestamp: Date.now(),
          finishReason: "stop",
          usage: {
            promptTokens,
            completionTokens,
            totalTokens: promptTokens + completionTokens
          }
        };
      }
    }
  }
  buildSummarizationPrompt(options) {
    let prompt = "You are a professional summarizer. ";
    switch (options.style) {
      case "bullet-points":
        prompt += "Provide a summary in bullet point format. ";
        break;
      case "concise":
        prompt += "Provide a very brief one or two sentence summary. ";
        break;
      case "paragraph":
      default:
        prompt += "Provide a clear and concise summary in paragraph format. ";
    }
    if (options.maxLength) {
      prompt += `Keep the summary under ${options.maxLength} words. `;
    }
    if (options.focus && options.focus.length > 0) {
      prompt += `Focus on: ${options.focus.join(", ")}. `;
    }
    prompt += `

Text to summarize:
${options.text}

Summary:`;
    return prompt;
  }
};
function createOllamaSummarize(model, host, options) {
  return new OllamaSummarizeAdapter(host, model, options);
}
function ollamaSummarize(model, options) {
  const host = getOllamaHostFromEnv();
  return new OllamaSummarizeAdapter(host, model, options);
}
export {
  OllamaSummarizeAdapter,
  OLLAMA_TEXT_MODELS as OllamaSummarizeModels,
  OllamaTextAdapter,
  OLLAMA_TEXT_MODELS as OllamaTextModels,
  createOllamaChat,
  createOllamaSummarize,
  ollamaSummarize,
  ollamaText
};
//# sourceMappingURL=@tanstack_ai-ollama.js.map
